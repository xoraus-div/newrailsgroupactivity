<%= form_with(model: doi, local: true) do |form| %>
  <% if doi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doi.errors.count, "error") %> prohibited this doi from being saved:</h2>

      <ul>
        <% doi.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :doidate %>
    <%= form.date_select :doidate %>
  </div>

  <div class="field">
    <%= form.label :animal_id %>
    <%= form.text_field :animal_id %>
  </div>

  <div class="field">
    <%= form.label :disease_id %>
    <%= form.text_field :disease_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
